<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Travel WebApp</title>
</head>
<H1>Edit Information Provided</H1>
<body>
    <form method="POST" action="/listing/<%=listing.id%>?_method=PUT">
        <input type="text" name="title" value="<%=listing.title%>" required>
        <br><br>
        <textarea name="description" required><%= listing.description %></textarea>
        <br><br>
        <input type="text" name="imageFilename" value="<%=listing.imageFilename%>" required>
         <br><br> -->
        <!-- <input type="text" name="url" value="<%=listing.image.url%>" required>
        <br><br>
        <input type="number" name="price" value="<%=listing.price%>">
        <br><br>
        <input type="text" name="country" value="<%=listing.country%>">
        <br><br>
        <input type="text" name="location" value="<%=listing.location%>">
        <br><br>
        <button type="submit">Submit</button>
    </form>
</body>
</html> --> 
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Information - Travel WebApp</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f9f9f9;
            margin: 0;
            padding: 0;
        }
        h1 {
            text-align: center;
            color: #333;
            margin-top: 30px;
        }
        .form-container {
            width: 50%;
            margin: 0 auto;
            padding: 20px;
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }
        form {
            display: flex;
            flex-direction: column;
        }
        input, textarea {
            margin-bottom: 15px;
            padding: 10px;
            font-size: 16px;
            border: 1px solid #ddd;
            border-radius: 4px;
            width: 100%;
        }
        textarea {
            height: 150px;
            resize: none;
        }
        button {
            padding: 10px 20px;
            font-size: 16px;
            background-color: #0056b3;
            color: white;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }
        button:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <h1>Edit Information Provided</h1>
    <div class="form-container">
        <form method="POST" action="/listing/<%=listing.id%>?_method=PUT">
            <input type="text" name="title" value="<%=listing.title%>" required placeholder="Enter title">
            <textarea name="description" required placeholder="Enter description"><%= listing.description %></textarea>
            <!-- <input type="text" name="imageFilename" value="<%=listing.imageFilename%>" required placeholder="Enter image filename"> -->
            <input type="text" name="url" value="<%=listing.image.url%>" required placeholder="Enter URL">
            <!-- <input type="number" name="price" value="<%=listing.price%>" placeholder="Enter price">  -->
            <input type="text" name="location" value="<%=listing.location%>" placeholder="Enter location">
            <input type="text" name="country" value="<%=listing.country%>" placeholder="Enter city">
            
            <button type="submit">Submit</button>
        </form>
    </div>
</body>
</html> 